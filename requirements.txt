# numpy < 2: camb (via halomod -> hmf -> camb) fails under numpy>=2 with
# "only 0-dimensional arrays can be converted to Python scalars" in
# camb.set_cosmology. See environment.yml for the recommended conda install.
numpy>=1.20,<2
scipy>=1.7.0
astropy>=5.0.0
colossus>=1.3.0
halomod>=1.4.0
